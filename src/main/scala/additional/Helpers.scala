package additional

object Helpers {
  def Max(f: Int, s: Int): Int={
    if(f > s){
       f
    }
    else
    {
        s
    }
  }
  def Min(f: Int, s: Int): Int={
    if(f < s){
      f
    }
    else
    {
      s
    }
  }
}
